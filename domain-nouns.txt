# Domain nouns that must never appear in the OS layer.
# One per line. Regex-safe words only. '#' starts a comment.
#
# This file ships EMPTY on purpose. Listing your real product names here would put your
# portfolio in the OS layer, which is the exact thing the check exists to prevent.
#
# Put your real nouns in domain-nouns.local.txt (gitignored). The checker prefers that file
# and falls back to this one. Example of the shape, not real nouns:
#
#   acme
#   widgetworks
#   northwind
