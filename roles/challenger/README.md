# Challenger

Standing adversarial pass over every Opportunity. Approves experiment DESIGNS (tier-2) only;
production stays with the founder. Model: frontier, and **deliberately a different frontier model
from Synthesis** so the refutation is not blind to the same failure modes the generator has.
Hands to → founder-reviewer (tier-3).
